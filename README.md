# Light-car-promax

这是一个视觉小车主仓库，当前已经连接独立 GitHub 远程。

## 主要内容

- `src/`、`include/`
- `CMakeLists.txt`、`package.xml`
- `build/`、`log/`
- 数据、配置和测试文件

## 备注

- 从当前目录状态看，它更适合作为 `light-car` 系列项目的主仓库。

## GitHub 建议

- 单独在本仓库内同步，不建议再混入根目录总仓库。
- 保留源码和说明，忽略 `build/`、`log/` 等生成目录。
